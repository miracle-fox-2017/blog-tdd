const Post = require('../models/postModel');

const create = (req, res) => {
  
}

const getAll = (req, res) => {
  
}

const destroy = (req, res) => {
  
}

module.exports = {
  create,
  getAll,
  destroy
};
